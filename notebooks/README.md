# Notebooks
```
01-model-training.ipynb       # Defines and trains a neural network on the dataset. Trained model is stored in '../models/'.
02-model-evaluation.ipynb     # Reads a model from '../models' and evaluates its accuracy on the test set.

```